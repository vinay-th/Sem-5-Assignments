# 5. Write a python program to Count the total number of digits in a number.

num = int(input("Enter a number: "))

# make the number into string and string len is count of digit
str_num = str(num)
print(len(str_num))